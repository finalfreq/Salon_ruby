<h1 class= "page-header text-center">Welcome to SuperCuts!</h1>

<% if @stylists.any?() %>
<div class="center">
<p class="lead">Here are our current stylists:</p>

  <ul>
    <% @stylists.each() do |stylist| %>
    <li><a href="/stylists/<%= stylist.id %>"> <%= stylist.name %></a></li>
    <% end %>
  </ul>
</div>
<% else %>

<h3>Currently there are no stylists: <a href="/stylists"> Add a Stylist here! </a></h3>
<h5> <em> A stylist must be added before any clients can be registered. </em></h5>

<% end %>

<% if @stylists.any?() %>

<h4 id="stylists">To see a list of current clients visit our  <a href="/clients">clients page </a></h4>
<h4 id="clients">To see a list of current stylists visit our <a href="/stylists">stylists page </a></h4>

<% end %>
